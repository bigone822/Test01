# MINI_MES 프로젝트 검토 및 확장 기획안

## 1. 종합 결론

현재 MINI_MES는 **MES의 핵심 개념을 학습하고 시연하기 위한 프로토타입**으로는 좋은 방향을 갖추고 있다.

현재 구현된 주요 업무 흐름은 다음과 같다.

```text
원자재 입고
    ↓
BOM 기반 생산 등록
    ↓
완제품 LOT 생성
    ↓
원자재 투입 이력 저장
    ↓
정방향·역방향 LOT 추적
    ↓
생산 현황 조회
```

다만 실제 현장 운영에 사용하려면 신규 기능을 추가하기 전에 **데이터 무결성, 재고 정확성, 실행 구조**를 먼저 안정화해야 한다.

현재 상태에서 기능만 확장하면 잘못된 재고와 불완전한 LOT 추적 데이터가 계속 누적될 가능성이 크다.

---

## 2. 현재 프로젝트 평가

| 영역 | 평가 | 설명 |
|---|---|---|
| 업무 흐름 | 양호 | 입고·생산·LOT 추적의 기본 흐름이 연결되어 있다. |
| 화면 구성 | 보통 | 조회·등록·대시보드가 있으나 실행 진입점과 페이지 구성이 혼재되어 있다. |
| 코드 구조 | 보통 | UI, 쿼리, 서비스가 일부 분리되어 있어 확장 기반은 있다. |
| 데이터 모델 | 초기 단계 | 품목·BOM·LOT·생산 중심이며 재고 이동·품질·설비 모델은 없다. |
| 데이터 무결성 | 위험 | 실제 DB에 외래키 위반과 추적 이력 누락이 존재한다. |
| 테스트·배포 | 미흡 | 테스트, 의존성 정의, 마이그레이션, README가 없다. |
| 현장 운영성 | 미흡 | 사용자·권한·감사 로그·동시성·백업 정책이 없다. |

### 2.1 잘된 부분

- DB 접근이 [`src/db.py`](src/db.py)로 모여 있다.
- 조회 SQL이 대부분 매개변수 방식으로 작성되어 있다.
- 생산 등록 시 완제품 LOT, 생산실적, 원자재 투입 이력을 하나의 트랜잭션으로 저장한다.
- [`src/services.py`](src/services.py)에 UI와 저장 로직을 분리하려는 방향이 보인다.
- `dataclass`를 사용하고 있어 향후 도메인 모델로 확장하기 좋다.
- 정방향·역방향 LOT 추적이라는 MES의 핵심 기능을 구현했다.

---

## 3. 우선 해결해야 할 문제

### 3.1 P0: 실제 DB 무결성 문제

실제 사용 DB인 [`sql/m_mes_ramen.db`](sql/m_mes_ramen.db)를 검사한 결과 다음 문제가 확인되었다.

- 외래키 위반 14건
- 생산실적 31건 중 21건에 원자재 투입 이력이 없음
- 원자재 입고량보다 계산상 사용량이 많은 LOT 2건
  - 면 블록: `-6,500`
  - 매운맛 스프: `-5,500`
- 일부 생산 투입 이력이 존재하지 않는 생산 또는 LOT를 참조
- BOM의 `bom_id`가 모두 `NULL`

따라서 현재 DB의 추적 결과와 재고 수량을 운영 데이터로 신뢰하기 어렵다.

### 권장 조치

1. 현재 DB를 별도 파일로 백업한다.
2. 외래키 위반 데이터를 식별해 복구하거나 테스트 데이터로 격리한다.
3. 생산실적과 원자재 투입 이력의 완전성을 검사하는 스크립트를 만든다.
4. 데이터 복구 후 `PRAGMA foreign_key_check`가 0건인지 확인한다.

### 3.2 P0: DB 초기화 SQL 문제

[`sql/schema.sql`](sql/schema.sql)은 `item`보다 `bom`을 먼저 생성하고, [`sql/sample_data.sql`](sql/sample_data.sql)도 품목보다 BOM을 먼저 입력한다.

외래키가 활성화된 새 DB에서 초기화하면 BOM 입력이 실패한다.

현재 BOM 기본키 정의도 SQLite에 적합하지 않다.

```sql
bom_id INT AUTO_INCREMENT PRIMARY KEY
```

SQLite에서는 다음과 같은 형태가 적합하다.

```sql
bom_id INTEGER PRIMARY KEY
```

### 권장 생성 순서

```text
item
  ↓
bom
  ↓
lot
  ↓
production
  ↓
production_material
```

샘플 데이터 역시 품목을 먼저 등록한 후 BOM과 LOT를 등록해야 한다.

### 3.3 P0: 실행 기준점 혼재

- 루트의 `m_mes_ramen.db`, `ramen_mes.db`는 0바이트이다.
- 실제 공통 DB 모듈은 `sql/m_mes_ramen.db`를 사용한다.
- 하지만 [`app.py`](app.py)는 루트의 빈 DB에 직접 연결한다.
- `app.py` 실행 시 `no such table: item` 오류가 발생한다.
- `app2.py`, `app3.py`, `app5.py`, `app_00.py`, `000.py`, `db2.py` 같은 실험 파일이 운영 코드와 섞여 있다.
- [`pages/05_생산이력.py`](pages/05_생산이력.py)는 빈 파일이다.

### 권장 조치

- `streamlit_app.py` 하나를 공식 실행점으로 지정한다.
- 현재 `pages/`는 `app_pages/`로 변경하고 `st.navigation`으로 메뉴를 명시적으로 구성한다.
- 실험 파일은 `archive/` 또는 `examples/`로 옮긴다.
- DB 경로는 환경설정 또는 하나의 공통 모듈에서만 관리한다.
- 빈 DB와 사용하지 않는 DB 사본은 백업 후 정리한다.

### 3.4 P1: 재고가 실제로 차감되지 않음

현재 생산 등록은 원자재 투입 이력을 기록하지만 원자재 재고를 차감하지 않는다.

자동 LOT 선택도 LOT 번호가 가장 빠른 행 하나를 선택할 뿐 다음 조건을 고려하지 않는다.

- 실제 잔량
- 필요량 충족 여부
- 유효기한
- 품질검사 합격 여부
- FIFO 또는 FEFO 정책
- 여러 LOT 분할 투입

이 상태에서는 생산 이력이 만들어져도 재고 장부는 정확하지 않다.

### 권장 조치

`lot.qty`를 직접 수정하는 방식보다는 `inventory_transaction` 테이블에 모든 재고 증감 이력을 기록하는 것이 좋다.

예시:

| 거래 유형 | 수량 | 설명 |
|---|---:|---|
| RECEIPT | `+5,000` | 원자재 입고 |
| MATERIAL_ISSUE | `-1,000` | 생산 투입 |
| PRODUCTION_RECEIPT | `+950` | 완제품 생산 입고 |
| SCRAP | `-50` | 폐기 |
| ADJUSTMENT | `±수량` | 재고 조정 |

현재고는 거래 이력의 합으로 계산한다.

### 3.5 기타 코드 개선점

- 원자재 입고 화면에 유효기한 입력이 중복되어 있다.
- 생산번호의 날짜가 사용자가 선택한 생산일자가 아니라 시스템 오늘 날짜를 사용한다.
- `MAX(id) + 1` 방식은 동시 등록 시 충돌할 수 있다.
- Streamlit에서 제거 예정인 `use_container_width`를 사용하고 있다.
- LOT 번호 생성 규칙이 품목명과 문자열 분리에 의존한다.
- 테스트, 마이그레이션 도구, 의존성 파일, 실행 설명서가 없다.
- 조회 결과의 컬럼명과 상태값이 사용자 친화적인 한글 표현으로 통일되지 않았다.

---

## 4. MINI_MES 제품 기획

### 4.1 제품 목표

> 소규모 식품 생산 공정에서 원자재 입고부터 완제품 생산과 LOT 추적까지 관리하는 경량 MES

### 4.2 주요 사용자

### 생산 작업자

- 작업지시 확인
- 작업 시작·중지·완료
- 원자재 LOT 확인
- 생산실적 및 불량수량 등록

### 자재 담당자

- 원자재 입고
- LOT·유효기한 관리
- 재고 이동 및 조정
- 부족 재고와 유효기한 임박 재고 확인

### 생산 관리자

- 생산계획 수립
- 작업지시 발행
- 계획 대비 실적 확인
- 작업 진행상태 모니터링

### 품질 담당자

- 입고검사
- 공정검사
- 완제품검사
- 부적합 LOT 보류 및 해제

### 시스템 관리자

- 품목·BOM·공정·설비 기준정보 관리
- 사용자와 권한 관리
- 감사 로그와 시스템 설정 관리

### 4.3 목표 업무 흐름

```text
품목·BOM 관리
    ↓
원자재 입고
    ↓
입고검사
    ↓
가용재고 등록
    ↓
생산계획
    ↓
작업지시 발행
    ↓
원자재 LOT 할당
    ↓
자재 투입
    ↓
생산실적 및 양품·불량 등록
    ↓
완제품 LOT 생성
    ↓
완제품 품질판정
    ↓
완제품 재고입고
    ↓
출하 및 고객 LOT 추적
```

---

## 5. 기능 모듈 기획

### 5.1 기준정보 관리

- 품목 관리
- 품목 유형 및 단위 관리
- BOM 및 BOM 버전 관리
- 공정·라우팅 관리
- 창고·보관위치 관리
- 설비·작업장 관리
- 사용자·역할 관리

### 5.2 자재·재고 관리

- 원자재 입고
- 입고검사 및 합격·보류·불합격 상태
- LOT별 재고
- 재고 이동 장부
- 가용·할당·보류 수량 구분
- FIFO/FEFO 출고
- 유효기한 임박 알림
- 재고 조정 및 조정 사유 기록

### 5.3 생산관리

- 생산계획
- 작업지시 발행
- 작업 시작·중지·완료
- 원자재 LOT 자동 할당
- 실제 원자재 투입량 등록
- 양품·불량·재작업 수량 등록
- 완제품 LOT 자동 발행
- 생산 취소 및 역분개

### 5.4 품질관리

- 입고검사
- 공정검사
- 완제품검사
- 검사 항목과 기준값 관리
- 불량유형과 원인 관리
- LOT 보류·해제·폐기

### 5.5 추적성 관리

- 원자재에서 완제품으로 정방향 추적
- 완제품에서 원자재로 역방향 추적
- 출하처와 완제품 LOT 연결
- 리콜 대상 LOT와 수량 산출
- LOT 계보 시각화
- 추적 데이터 완전성 검사

### 5.6 생산 대시보드

- 계획 대비 생산실적
- 품목별·일자별 생산량
- 양품률과 불량률
- 작업지시 진행상태
- 재고 부족 현황
- 유효기한 임박 LOT
- 품질 보류 LOT
- LOT 추적 완전성

설비 가동시간과 비가동 사유 데이터가 확보되기 전에는 OEE를 계산하지 않는 것이 좋다.

---

## 6. 단계별 확장 로드맵

| 단계 | 목표 | 핵심 작업 | 완료 기준 |
|---|---|---|---|
| 0단계 | 기반 안정화 | DB 복구, 스키마 수정, 공식 실행점 확정, 실험 파일 분리 | 외래키 위반 0건, 새 DB 자동 생성 성공 |
| 1단계 | 재고 정확성 | 재고 이동 장부, 잔량 계산, FEFO 할당, 다중 LOT 투입 | 음수 재고 방지, 모든 투입 이력 추적 |
| 2단계 | 생산 실행 | 생산계획, 작업지시, 상태 전환, 양품·불량 등록 | 계획부터 완료까지 업무 흐름 연결 |
| 3단계 | 품질·추적 | 검사결과, LOT 보류, 출하 연결, 리콜 범위 계산 | 원자재부터 출하처까지 추적 가능 |
| 4단계 | 운영 시스템화 | 로그인, 권한, 감사 로그, PostgreSQL, 백업 | 다중 사용자 운영과 변경 이력 보장 |
| 5단계 | 스마트팩토리 연계 | PLC·센서, 설비상태, 비가동, 예지보전 | 설비 데이터 기반 실시간 현황 제공 |

---

## 7. 권장 데이터 모델

### 7.1 우선 추가할 테이블

```text
inventory_transaction
production_order
production_result
quality_inspection
warehouse
location
user
audit_log
```

### 7.2 중기적으로 추가할 테이블

```text
bom_header
bom_revision
bom_line
routing
routing_operation
work_center
equipment
defect
downtime
shipment
shipment_lot
```

### 7.3 중요한 데이터 원칙

- LOT 수량은 재고 이동 이력과 일치해야 한다.
- 완료된 생산 데이터는 직접 삭제하지 않고 취소 또는 역분개한다.
- BOM은 생산 당시의 버전을 보존해야 한다.
- 모든 변경에는 작업자와 변경 시간이 기록되어야 한다.
- 원자재 투입은 품질 합격·유효기한·가용재고 조건을 만족해야 한다.
- 추적에 필요한 데이터는 같은 트랜잭션에서 함께 저장해야 한다.

---

## 8. OOP 적용 방향

### 8.1 결론

MINI_MES에는 **선택적으로 OOP를 적용하는 것이 좋다.**

현재 코드를 전부 클래스로 다시 작성하는 전면적인 OOP 전환은 권장하지 않는다.

Streamlit 페이지와 단순 조회 함수는 현재처럼 절차형 코드가 더 읽기 쉽다. 반면 상태와 업무 규칙이 복잡한 영역은 객체로 분리하는 것이 좋다.

### 8.2 OOP 적용이 필요한 영역

- 생산 등록·완료·취소
- 재고 입고·할당·투입·조정
- FIFO/FEFO 정책
- 품질 판정
- LOT 및 생산번호 발행
- 트랜잭션 관리
- SQLite와 PostgreSQL 저장소 교체

### 8.3 OOP가 필요한 이유

MES는 단순한 CRUD 시스템보다 **상태와 업무 규칙**이 중요하다.

예를 들면 다음 규칙이 있다.

- 유효기한이 지난 LOT는 투입할 수 없다.
- 검사 합격 LOT만 생산에 사용할 수 있다.
- 가용재고보다 많은 수량을 투입할 수 없다.
- 생산 완료 시 원자재 출고와 완제품 입고가 함께 성공해야 한다.
- 완료된 생산은 직접 삭제하지 않고 취소 이력을 생성해야 한다.
- 생산 당시 사용한 BOM 버전을 보존해야 한다.

이러한 규칙을 화면과 SQL 함수에 흩어 놓으면 화면마다 검증 결과가 달라질 수 있다.

도메인 객체와 서비스에 규칙을 모으면 다음 이점이 있다.

- 모든 화면이 동일한 업무 규칙을 사용한다.
- 업무 규칙을 DB 없이 단위 테스트할 수 있다.
- UI 변경이 업무 로직에 미치는 영향을 줄인다.
- SQLite에서 PostgreSQL로 전환하기 쉬워진다.
- FIFO, FEFO 같은 정책을 교체하기 쉬워진다.

### 8.4 권장 프로젝트 구조

```text
mini_MES/
├── streamlit_app.py
├── app_pages/
│   ├── dashboard.py
│   ├── material_receipt.py
│   ├── production_order.py
│   ├── production_registration.py
│   ├── inventory.py
│   └── traceability.py
├── application/
│   ├── production_service.py
│   ├── inventory_service.py
│   └── traceability_service.py
├── domain/
│   ├── item.py
│   ├── lot.py
│   ├── inventory.py
│   ├── production.py
│   ├── quality.py
│   └── policies.py
├── infrastructure/
│   ├── database.py
│   ├── unit_of_work.py
│   └── repositories/
├── migrations/
├── tests/
└── README.md
```

### 8.5 권장 클래스

| 클래스 | 역할 |
|---|---|
| `ProductionService` | 생산 등록·완료·취소 업무 처리 |
| `InventoryService` | 입고·할당·투입·조정 처리 |
| `TraceabilityService` | 정방향·역방향·리콜 추적 |
| `LotAllocationPolicy` | LOT 할당 정책 인터페이스 |
| `FEFOPolicy` | 유효기한 우선 LOT 할당 |
| `LotRepository` | LOT 저장·조회 인터페이스 |
| `ProductionRepository` | 생산 저장·조회 인터페이스 |
| `SqliteUnitOfWork` | SQLite 트랜잭션 경계 관리 |
| `LotNumberGenerator` | LOT 및 생산번호 생성 |

깊은 상속 구조보다는 다음 기능을 활용하는 것이 적합하다.

- `dataclass`
- `Enum`
- `Protocol`
- 객체 조합
- 의존성 주입

### 8.6 권장 계층 구조

```text
Streamlit UI
    ↓
Application Service
    ↓
Domain Model / Policy
    ↓
Repository Interface
    ↓
SQLite 또는 PostgreSQL
```

UI는 입력과 출력만 담당하고, 업무 규칙과 트랜잭션은 Application Service에서 처리해야 한다.

---

## 9. 테스트 전략

### 9.1 단위 테스트

- LOT 번호 생성
- 유효기한 검증
- 생산수량 검증
- FIFO/FEFO LOT 선택
- 가용재고 초과 방지
- BOM 필요수량 계산
- 생산 상태 전환

### 9.2 DB 통합 테스트

- 입고 시 LOT와 재고 거래가 함께 생성되는지 확인
- 생산 완료 시 원자재 출고와 완제품 입고가 함께 처리되는지 확인
- 중간 오류 발생 시 전체 트랜잭션이 롤백되는지 확인
- 정방향·역방향 추적 결과가 일치하는지 확인
- 외래키 위반이 발생하지 않는지 확인

### 9.3 화면 테스트

- 페이지가 예외 없이 실행되는지 확인
- 필수 입력 검증
- 저장 버튼 중복 클릭 방지
- 데이터가 없는 화면 처리
- 사용자 권한별 메뉴 노출

---

## 10. 운영 전환 방향

### 교육·개인 시연 단계

- Streamlit
- SQLite
- 단일 사용자
- 로컬 실행

### 소규모 현장 운영 단계

- Streamlit UI
- PostgreSQL
- 사용자 로그인과 역할별 권한
- DB 마이그레이션
- 자동 백업
- 감사 로그

### 다중 공정·설비 연계 단계

- Streamlit은 대시보드와 관리자 화면 중심으로 사용
- 별도 API 서버 도입
- 설비·PLC·센서 연계
- 작업 단말용 UI 분리
- 메시지 큐 또는 이벤트 처리 도입

SQLite는 교육용·단일 사용자 환경에는 적합하지만 여러 작업자가 동시에 생산과 입고를 등록하는 환경에서는 PostgreSQL 전환을 권장한다.

---

## 11. 최종 실행 우선순위

1. 현재 DB 백업 및 무결성 복구
2. `streamlit_app.py`를 공식 실행점으로 지정
3. 스키마·샘플 데이터·DB 생성 절차 수정
4. 실험 파일과 실제 운영 파일 분리
5. 재고 이동 장부 구현
6. FEFO 기반 LOT 자동 할당 구현
7. 핵심 서비스부터 선택적으로 OOP 적용
8. 단위 테스트와 DB 통합 테스트 추가
9. 생산계획·작업지시 기능 추가
10. 품질·출하·리콜 기능 확장

---

## 12. 최종 판단

현재 프로젝트는 폐기하거나 처음부터 다시 만들어야 하는 단계는 아니다.

이미 다음과 같은 중요한 기반이 마련되어 있다.

- 입고
- 생산
- BOM
- LOT
- 투입 이력
- 정방향·역방향 추적
- 생산 대시보드

따라서 현재 구조를 보존하면서 데이터 무결성과 재고 장부를 먼저 안정화하고, 업무 규칙이 복잡해지는 영역부터 선택적으로 OOP를 적용하는 것이 가장 현실적인 방향이다.

MINI_MES의 다음 목표는 단순히 화면을 늘리는 것이 아니라 다음 세 가지를 보장하는 시스템으로 발전하는 것이다.

> **재고가 정확하고, 생산 이력이 완전하며, 모든 LOT를 끝까지 추적할 수 있는 MES**
